<Window x:Class="ChatViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <Style x:Key="HightlightedListViewItem" TargetType="ListViewItem">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                    <Setter Property="Foreground" Value="Red"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Menu Grid.Row="0" VerticalAlignment="Center">
            <MenuItem Header="_File">
                <MenuItem Header="_Open" Command="{Binding FileOpenCommand}"/>
            </MenuItem>
        </Menu>

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
            <TextBlock Text="FilePath: "/>
            <TextBlock Grid.Row="1" Text="{Binding FileName}"/>
        </StackPanel>

        <Grid Grid.Row="2" Margin="8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <TextBox Text="{Binding SearchParam, UpdateSourceTrigger=PropertyChanged}" Height="25" VerticalAlignment="Center" VerticalContentAlignment="Center" Margin="0,0,8,0">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            <Button Command="{Binding SearchCommand}" Grid.Column="1" Height="25" VerticalAlignment="Center">Search</Button>
        </Grid>

        <ListView Grid.Row="3" ItemsSource="{Binding ChatLog}" ItemContainerStyle="{StaticResource HightlightedListViewItem}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Time" DisplayMemberBinding="{Binding Time}"/>
                    <GridViewColumn Header="Sender" DisplayMemberBinding="{Binding Sender}"/>
                    <GridViewColumn Header="Message" DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
